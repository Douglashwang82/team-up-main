[phases.setup]
nixPkgs = ["python310"]
aptPkgs = ["postgresql-client"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn wsgi:app --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 120 --log-level info --access-logfile -"
